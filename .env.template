# CADP Connection Configuration
CADP_API_HOST=192.168.100.20
CADP_API_PORT=443
CADP_API_TLS=false

# Authentication
CADP_REGISTRATION_TOKEN=j96WogOWbdLPkucAtAQyp9Le6zaSvuTx611bCY1acOJlHUWqTHZbJmNzy09VF9bx
CADP_USER_NAME=dev-user01

# Protection Defaults (used if no specific policy provided per column)
# Note: In CadpClient integrated mode, this maps to the default policy if needed, 
# though Config.java has its own default "dev-users-policy" if not set.
# Config.java logic: defaultPolicy = "dev-users-policy".
# But we should probably allow overriding it via env.
# Config.java doesn't seem to read CADP_PROTECTION_POLICY_NAME specifically for defaultPolicy, 
# it has "private String defaultPolicy = "dev-users-policy";" and setters.
# Let's check Config.java again to be sure if it reads a variable for policy.
